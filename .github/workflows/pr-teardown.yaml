name: PR Teardown on Dev

on:
  pull_request:
    types: [unlabeled]

jobs:
  build:
    if: contains(github.event.pull_request.labels.*.name, 'build')
    name: PR Teardown
    runs-on: ubuntu-latest

    steps:

      - name: Run PR Teardown
        run: echo "PR Teardopwn on Dev ..."